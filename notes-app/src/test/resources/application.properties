# Use in-memory repository in tests to avoid DB bootstrap requirements.
persistence=in-memory

# Docker is not required for tests on this setup.
quarkus.devservices.enabled=false
quarkus.datasource.devservices.enabled=false

# ORM is not needed when tests run with in-memory repositories.
quarkus.hibernate-orm.enabled=false

